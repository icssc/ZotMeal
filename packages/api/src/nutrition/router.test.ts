import { apiTest } from "@api/apiTest";
import { describe } from "vitest";

// TODO: finish adding tests

describe("testLogMeal", () => {
  apiTest.todo("test the logMeal trpc");
});

describe("testGetAllLoggedMeals", () => {
  apiTest.todo("test the getAllLoggedMeals trpc");
});

describe("testDeleteLoggedMeal", () => {
  apiTest.todo("test the deleteLoggedMeal trpc");
});