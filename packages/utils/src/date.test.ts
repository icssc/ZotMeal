import { describe, expect, it } from "vitest";

describe("parseDate parses properly", () => {
  it("returns correct date on parse", () => {
    expect(true).toBe(true);
  });

  it("returns null on incorrect date format", () => {
    expect(true).toBe(true);
  });
});
